{
  "_comment": "Claude Desktop MCP config — copy the mcpServers block into your claude_desktop_config.json",
  "mcpServers": {
    "linkedin-sales-navigator": {
      "command": "node",
      "args": ["./mcp/linkedin_mcp/dist/index.js"],
      "env": {
        "LI_AT_COOKIE": "YOUR_LI_AT_COOKIE_HERE"
      }
    }
  }
}
